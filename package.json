{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "test": "node tests/invariants.mjs"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
