[package]
name = "tween"
version = "0.1.0"
entrypoint = "lib.typ"
authors = ["Uwni"]
license = "MIT"
description = "Several states of one Typst drawing, and the browser moves between them: element animation for HTML export, still in the PDF."
repository = "https://github.com/uwni/vit-typ"
keywords = ["animation", "html", "svg", "interpolation", "figure"]
categories = ["visualization"]
compiler = "0.15.0"
