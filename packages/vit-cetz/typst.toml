[package]
name = "vit-cetz"
version = "0.1.0"
entrypoint = "lib.typ"
authors = ["Uwni"]
license = "MIT"
description = "CeTZ for vit: cetz.draw with an identity on every element, so one canvas can carry several marks and animate inside itself."
repository = "https://github.com/uwni/vit-typ"
keywords = ["slides", "presentation", "html", "animation", "cetz"]
categories = ["presentation"]
compiler = "0.15.0"
